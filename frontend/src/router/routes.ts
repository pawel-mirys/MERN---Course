const routes = {
  root: '/',
  product: '/product/:id',
  cart: '/cart',
  login: '/login',
  register: '/register',
  shipping: '/shipping',
  payment: '/payment',
  placeOrder: '/placeorder',
  order: '/order/:id',
  profile: '/profile',
};

export default routes;
